<div class="media">

  <div class="media-left">
    <%= link_to gallery.pictures do %>
      <%= image_tag gallery.pictures.first, class: "media-object img-circle" %>
    <% end %>
  </div>

  <h3><%= @gallery.name %></h3>

  <p><%= @gallery.description %></p>
